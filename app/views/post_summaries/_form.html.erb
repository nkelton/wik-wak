<%= form_with(model: post_summary, local: true) do |form| %>
  <% if post_summary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_summary.errors.count, "error") %> prohibited this post_summary from being saved:</h2>

      <ul>
        <% post_summary.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :up_votes %>
    <%= form.text_field :up_votes %>
  </div>

  <div class="field">
    <%= form.label :down_votes %>
    <%= form.text_field :down_votes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
